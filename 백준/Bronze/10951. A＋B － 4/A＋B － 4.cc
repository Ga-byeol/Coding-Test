#include<stdio.h>

main()
{
    int A,B,x=0,y=0;
    while(scanf("%d %d",&A,&B)!=EOF)
    {
        
        //if(A==x&&B==y) break;
        //x=A;
        //y=B;
        printf("%d\n",A+B);
    }
}